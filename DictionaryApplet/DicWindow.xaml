<Window x:Class="PersonalDictionary.DicWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Словарь" Height="600" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Ribbon x:Name="RibbonWin" SelectedIndex="0">
            <Ribbon.ApplicationMenu>
                <RibbonApplicationMenu Visibility="Hidden"></RibbonApplicationMenu>
            </Ribbon.ApplicationMenu>
            <RibbonTab Header="Главное">
                <RibbonGroup Header=""  VerticalAlignment="Stretch" HorizontalAlignment="Stretch" >
                    <StackPanel Orientation="Horizontal">
                        <Grid Width="300">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="40"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Англ." Margin="0,0,5,0" Grid.Column="0" Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Center" />
                            <TextBlock Text="Рус." Margin="0,0,5,0" Grid.Column="0" Grid.Row="1" HorizontalAlignment="Left" VerticalAlignment="Center"  />
                            <TextBox Text="" x:Name="en_tb" Grid.Column="1" Grid.Row="0" HorizontalAlignment="Stretch"  VerticalAlignment="Stretch" Margin="1" FontSize="12" Height="24" TextChanged="en_tb_TextChanged"/>
                            <TextBox Text="" x:Name="ru_tb" Grid.Column="1" Grid.Row="1" HorizontalAlignment="Stretch"  VerticalAlignment="Stretch" Margin="1" FontSize="12" Height="24" TextChanged="en_tb_TextChanged" />
                            <!--<RibbonButton SmallImageSource="img\mines.png" Label="  Удалить слово"  Click="del_word_Click"/>-->
                        </Grid>
                        <RibbonButton LargeImageSource="img\plus.png" Label="добавить" Click="add_word_Click"/>
                    </StackPanel>

                </RibbonGroup>
                <RibbonGroup Header=""  VerticalAlignment="Stretch" HorizontalAlignment="Stretch" >
                    <RibbonButton LargeImageSource="img\mines.png" Label="удалить" Click="del_word_Click"/>
                </RibbonGroup>
                <RibbonGroup Header=""  VerticalAlignment="Stretch" HorizontalAlignment="Stretch" >
                    <RibbonButton LargeImageSource="img\edit.png" Label="изменить" Click="edit_word_Click"/>
                </RibbonGroup>
            </RibbonTab>
        </Ribbon>
        <DataGrid Name="dataGrid" Grid.Row="1" AutoGenerateColumns="False" FontSize="14" SelectionMode="Extended"  
                  SelectionUnit="FullRow"  IsReadOnly="True">
            <DataGrid.GroupStyle>
                <GroupStyle>
                    <GroupStyle.ContainerStyle>
                        <Style TargetType="{x:Type GroupItem}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type GroupItem}">
                                        <Expander IsExpanded="True">
                                            <Expander.Header>
                                                <TextBlock Text="{Binding Path=Name}"/>
                                            </Expander.Header>
                                            <ItemsPresenter />
                                        </Expander>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </GroupStyle.ContainerStyle>
                    <GroupStyle.Panel>
                        <ItemsPanelTemplate>
                            <DataGridRowsPresenter/>
                        </ItemsPanelTemplate>
                    </GroupStyle.Panel>
                </GroupStyle>
            </DataGrid.GroupStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Слово (англ.)" Binding="{Binding En}" MinWidth="50" />
                <DataGridTextColumn Header="Перевод (рус.)" Binding="{Binding Ru}" MinWidth="150" />
                <DataGridTextColumn Header="Добавлено" Binding="{Binding Add}" MinWidth="150" />
                <DataGridTextColumn Header="Изменено" Binding="{Binding Modified}" MinWidth="150" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
